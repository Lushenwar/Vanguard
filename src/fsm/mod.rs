//! Deterministic finite state machine: the vocabulary, the legal edge set, and
//! the pure evaluator that decides whether a proposal may be applied.

pub mod engine;
pub mod state;
pub mod transition;
